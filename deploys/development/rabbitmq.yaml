image:
  debug: true

replicaCount: 1

rabbitmq:
  username: keystone-user
  existingPasswordSecret: rabbitmq-secrets

persistence:
  enabled: true
  size: 5Gi

ingress:
  enabled: true
  hostName: "rabbitmq-staging.summn.co"

resources:
  requests:
    memory: 256Mi
    cpu: 125m
