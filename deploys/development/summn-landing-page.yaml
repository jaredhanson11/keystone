resources:
  requests:
    memory: 256Mi
    cpu: 125m

image:
  imageVersion: 1.1.1

server:
  image:
    imageVersion: latest
  rmqConnection:
    passwordSecret:
      secretName: staging-secrets
      keyName: rabbitmq-password
    host: rabbitmq

ingress:
  subdomain: "staging"
