replicaCount: 1

auth:
  username: keystone-user
  existingPasswordSecret: rabbitmq-secrets

persistence:
  enabled: true
  size: 5Gi

ingress:
  enabled: true
  hostname: "rabbitmq.summn.co"

resources:
  requests:
    memory: 256Mi
    cpu: 125m
