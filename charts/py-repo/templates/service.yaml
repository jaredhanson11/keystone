apiVersion: v1
kind: Service
metadata:
  name: {{ include "endergy-lib.common.fullname" . }}
  labels:
    {{- include "endergy-lib.common.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "endergy-lib.common.selectorLabels" . | nindent 4 }}
