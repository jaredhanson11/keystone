apiVersion: v1
kind: Service
metadata:
  name: {{ include "py-repo.fullname" . }}
  labels:
    {{- include "py-repo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.deployment.port }}
      protocol: TCP
      name: http
  selector:
    {{- include "py-repo.selectorLabels" . | nindent 4 }}
