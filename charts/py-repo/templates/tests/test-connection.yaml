apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "py-repo.fullname" . }}-test-connection"
  labels:
{{ include "py-repo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "py-repo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
